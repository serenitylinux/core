# vim: ft=sh
name="mpfr"
version="3.1.2"
desc="Multiple-precision floating-point library"
url="http://www.mpfr.org/"
src="http://www.mpfr.org/mpfr-current/mpfr-${version}.tar.xz"
bdeps="gmp"
deps="gmp"
arch="any"
flags=""
srcdir="$name-$version"


function configure() {
	./configure --prefix=/usr --enable-thread-safe --enable-shared
}

function build() {
	default
}

function installpkg() {
	default
}
